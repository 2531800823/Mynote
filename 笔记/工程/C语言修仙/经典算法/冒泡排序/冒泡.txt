#define _CRT_SECURE_NO_WARNINGS

#include<stdio.h>
int main(void)
{
	int a[3] = { 12,58,10 };
	for (int i = 0; i < 3; i++){
		for (int j = i + 1; j < 3; j++) {
			if (a[i] > a[j]) {
				int W = a[i];
				a[i] = a[j];
				a[j] = W;
			}
		}
	}
	for (int i = 0; i < 3; i++)
		printf("%d\n",a[i]);

	return 0;
}