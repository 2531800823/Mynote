#define _CRT_SECURE_NO_WARNINGS

#include<stdio.h>
int main(void)
{
	int n, a, i, num;
	scanf("%d %d", &n, &a);
	for (i = 1; i <= n && i <= a; i++) {
		if (n%i == 0 && a%i == 0)
			num = i;
	}
	printf("%d\n", num);
	return 0;e
}